def application(env, start_response):
    import sys
    start_response('200 OK', [('Content-Type','text/html')])
    return [ "Jelastic Hello World Python application, Python version - %s" % sys.version ]
